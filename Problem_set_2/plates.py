def main():
    plate = input("Plate: ")
    if is_valid(plate):
        print("Valid")
    else:
        print("Invalid")


def is_valid(s):
    len(plate) > 6 and len(plate) < 2
    plate.startswith(a number) or plate[1] == num 

main()
