# ask the user for input
x = input("what do you want to calc?\n")

# format the input
y = x.split()
# calc the input
a, b, c = int(y[0]), y[1], int(y[2])
# output
# how do i convert the string type plus to the operator type plus.
